apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-test-frontend"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test-frontend
      image: curlimages/curl
      command: ['curl']
      args:
      - --fail
      - --write-out
      - '%{http_code}'
      - --silent 
      - --output
      - /dev/null
      - 'http://{{ include "CID.fullname" . }}-frontend-service:80/'
  restartPolicy: Never
